# Required by src/ (the adaptive user-model schema/data layer):
pydantic>=2

# Required by src/api/ + tools/serve_api.py (local internal-alpha HTTP API
# wrapper around the existing repository/model functions). httpx is used by
# fastapi.testclient in tests/api/.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27

# Required by tools/document_reader/ for full-fidelity DOCX/XLSX/PPTX/PDF parsing:
openpyxl>=3.1
python-docx>=1.1
python-pptx>=0.6.23
pymupdf>=1.24
# Optional but preferred for richer PDF conversion when available:
# docling>=2.0

